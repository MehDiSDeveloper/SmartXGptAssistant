{
  "name": "GetWeather",
  "description": "Get the current weather in a given location",
  "parameters": {
    "type": "object",
    "properties": {
      "location": {
        "type": "string",
        "description": "The city and state, e.g. San Francisco, CA"
      },
      "unit": {
        "type": "string",
        "enum": [
          "celsius",
          "fahrenheit"
        ]
      },
      "userId": {
        "type": "string",
        "description": "the Id of the asking user wich assistant must get from provided id by system"
      }
    },
    "required": [
      "location",
      "unit",
      "userId"
    ]
  }
}